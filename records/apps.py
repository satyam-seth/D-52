from django.apps import AppConfig


class RecordsConfig(AppConfig):
    """Configuration class for the 'records' Django app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "records"
